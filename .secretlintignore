# Fichiers à exclure du scan secretlint (volume / faux positifs connus).
# Secretlint scan le contenu des fichiers stagés — ces patterns sont du bruit.

node_modules/
dist/
build/
target/
.git/

# Lockfiles : contiennent des hashs SHA qui ressemblent à des secrets
package-lock.json
yarn.lock
pnpm-lock.yaml
Cargo.lock

# Fichiers générés
apps/frontend/src-tauri/gen/
*.min.js
*.min.css

# Tests (peuvent contenir des fixtures avec faux secrets)
**/*.test.ts
**/*.test.tsx
**/*.spec.ts
**/fixtures/

# Docs avec exemples pédagogiques
docs/
GUIDE_DEPLOIEMENT_ATHENIS.md
CLAUDE.md
INSTALL_DATABASE.md

# Seed scripts (mots de passe démo intentionnels)
apps/backend/prisma/seed.ts
apps/backend/prisma/reset-demo-passwords.mjs

# .env.example contiennent des placeholders explicites
.env.example
.env.production.example
apps/frontend/.env.example
apps/frontend/.env.production
